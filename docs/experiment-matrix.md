# Experiment matrix

Historical graphics experiments E00-E04 retain `OMP_NUM_THREADS=16` and
`KMP_AFFINITY=disabled`. E05 starts a new batch on the D10 Wine-NUMA build
without either override. Each row requires two runs with the same scene and
settings. Until both runs exist, the classification remains **inconclusive**.

Allowed final values: **fixed**, **improved**, **unchanged**, **regressed**,
**inconclusive**.

## Graphics controls

| ID | Single changed variable | Run 1 | Run 2 | Starts | Menu aircraft | Flickering squares | Terrain loading | Distant terrain | Performance | Classification | Notes/evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|
| E00 | Baseline; no VKD3D diagnostic option | [completed](evidence-e00-baseline.md) | [completed](evidence-e00-baseline.md) | yes | blocks/artifacts present both runs | present both runs (user-observed) | severe tiled failure both runs | black/absent both runs | no material difference observed; stills only | unchanged | Repeatable baseline; no device loss/OOM; counts differ with run duration |
| E01 | `VKD3D_CONFIG=no_upload_hvv` | [completed](evidence-e01-no-upload-hvv.md) | [completed](evidence-e01-no-upload-hvv.md) | yes | inconclusive | inconclusive | more vegetation at low altitude; control effect unproven | still severely broken | no material change reported; stills only | inconclusive | Upload change confirmed twice, but altitude confounds visual comparison; matched-altitude A/B required |
| E02 | `VKD3D_CONFIG=single_queue` | [completed](evidence-e02-single-queue.md) | [completed](evidence-e02-single-queue.md) | yes | unchanged | unchanged | unchanged | unchanged | no material change reported; stills only | unchanged | Async compute/transfer queues disabled; both runs unchanged; no device loss/OOM |
| E03 | `VKD3D_DISABLE_EXTENSIONS=VK_EXT_descriptor_buffer` | [completed on D03-derived tool](evidence-e03-no-descriptor-buffer.md) | stock-Proton confirmation prepared | yes | unchanged | unchanged | unchanged; altitude dependency remains | unchanged | stills only; not controlled | inconclusive | Extension disable verified, but Steam remained on telemetry-derived `cfca234e`; gates were off. Confirm once on Proton Experimental |
| E04 | `VKD3D_CONFIG=no_upload_hvv,single_queue` | not run | not run | — | — | — | — | — | — | inconclusive | Runtime testing ended after E02; combination was not evaluated |
| E05 | `VKD3D_DISABLE_EXTENSIONS=VK_KHR_fragment_shading_rate` on D10, with no OpenMP override | [completed](evidence-e05-no-vrs-result.md) | not required before moving to focused pass tracing | yes | unchanged | unchanged | not tested | not tested | no material change reported | unchanged for the reproduced menu symptom | Build `24615759`; 228 verified disable warnings; no device-loss/OOM/reset signature. VRS is not required for the artifact |

## Development controls

| ID | Single changed variable | Status | Required observation | Classification |
|---|---|---|---|---|
| D00 | Prefix copy of locally compiled, otherwise unmodified VKD3D-Proton at `3dfc6f07…` | [invalid](evidence-d00-local-build.md) | Stock Proton recopies packaged DLLs at launch; runtime local-build parity was not established | inconclusive |
| U00 | Game build `24596901`; Proton-supplied VKD3D at `3dfc6f07…` | [completed](evidence-u00-game-update.md) | Same menu and terrain corruption; update did not change the defect | unchanged |
| D01a | Prefix copy plus `VKD3D_IL2_RESOURCE_TRACE=1`; trace-only build at `d0b4421f…` | [invalid](evidence-d01-invalid-prefix-install.md) | No trace marker; post-run hashes exactly match packaged Proton DLLs | inconclusive |
| D01b | Dedicated custom Proton plus the same trace build | [completed](evidence-d01b-sparse-trace.md) | Valid marker and diagnostic hashes; zero reserved-resource or tile-mapping calls | unchanged visually; sparse path excluded |
| D02 | Ordinary texture, mip-view, upload-copy, and lifetime census at local commit `54797ad3…` | [completed](evidence-d02-ordinary-texture-trace.md) | Valid marker/hashes; 2,355 complete compressed mip chains, zero partial, and 405 pre-cap SRV-bearing BC3 textures without a logged incoming upload/copy | unchanged visually; resource class narrowed |
| D03 | Placed-resource heap ranges and alias-barrier correlation at local commit `cfca234e…` | [completed](evidence-d03-alias-trace.md) | Valid markers/hashes; all 585 pre-cap candidates matched and none overlap any traced placed-resource range; zero explicit legacy alias barriers | unchanged visually; placed-resource alias path excluded |
| D04 | Unmodified current VKD3D-Proton `84c87c83…` with `dxil-spirv` `cc75a0c9…` | [completed](evidence-d04-upstream-result.md) | Post-run DLL hashes match D04; menu blocks and severe 6,400 m terrain-page loss remain | unchanged; current-upstream lead closed |
| D05a | Gated normalization of only the observed internal one-texel BC3 baked-terrain border copies; commit `35bd875c…` | [completed but invalid](evidence-d05-result.md) | Correct DLLs and one enable marker, but zero adjustments; unchanged screenshot therefore cannot test the behavior | inconclusive; source matcher too strict |
| D05b | Footprint-aware revision with candidate/rejection telemetry; commit `f6416c79…` | [completed](evidence-d05b-result.md) | 432 candidates, zero adjustments, 432 explicit rejections; all sources are footprint-only `R32G32B32A32_UINT` | inconclusive visually; source model disproved |
| D05c | Exact observed `R32G32B32A32_UINT` footprint to BC3 border reinterpret mapping; commit `5391ec7f…` | [completed](evidence-d05c-result.md) | 202/202 thin candidates adjusted, zero rejected, eight destinations; missing pages and magenta edges remain | unchanged; border-only correction excluded |
| D06 | Focused producer-to-SRV trace for 2048x2048 one-mip baked-terrain caches and associated intermediates; commit `376652dc…` | [completed](evidence-d06-result.md) | 292 `64x64` interiors emitted on a 256-texel grid plus 708 borders; active-cache coverage is 3.46-6.32%, versus 54.69-100% under the indicated 4x block mapping | unchanged visually; strong full-page reinterpret lead |
| D07 | Gated full-page `R32G32B32A32_UINT` to BC3 reinterpret conversion for six observed shapes; commit `833cafa0…` | [completed, two runs](evidence-d07-result.md) | Run 1: 522/522 adjustments; run 2: 304/304; zero rejects in both. Terrain is continuous near 5,500 m in both runs; run 2 confirms menu blocks/shimmering remain | improved overall; terrain fixed repeatably, menu unchanged |
| D08 | General buffer-to-image block-unit conversion plus regression test; commit `cf11ba76…` | [completed](evidence-d08-result.md) | Clean build identity, zero D07 markers; continuous detailed terrain at 4,813 m, 2,427 m, and 742 m; menu blocks/shimmering unchanged; no device loss/OOM | improved overall; terrain fixed, menu unchanged |
| D11 | Bounded resource-name and PIX pass-label telemetry; commit `d3cba21d…` | [completed](evidence-d11-menu-pass-trace-result.md) | 3,145 resource names; native-resolution SSR/current/history targets allocated; no PIX events; symptom unchanged | diagnostic only; reflection allocation confirmed, use unproven |
| D12 | Named reflection-resource usage, barrier, draw/shader, and submission telemetry; commit `e7f6e303…` | [completed](evidence-d12-reflection-usage-result.md) | 100,000 events over about 2,003 repeated cycles prove active SSR/reflection rendering with stable shaders and explicit non-split transitions; artifact unchanged | diagnostic only; simple missing transition on named SSR targets weakened |
| D13 | Focused `rtLightRefs`/self-light/light-volume barrier, clear, dispatch/draw shader, and submission telemetry; commit `395d9747…` | [completed](evidence-d13-light-grid-trace-result.md) | 200,000 events cover more than 1,500 stable final-generation cycles: self-light is cleared every frame and the tiled light grid has explicit state/UAV barriers; nearby consumers are the D12 reflection-pass shaders; symptom unchanged | diagnostic only; simple missing-clear/barrier form weakened, shader/descriptor interpretation remains open |
| D14 | Upstream `VKD3D_SHADER_DUMP_PATH` capture and static inspection of the exact tiled-light and correlated graphics shaders | [completed](evidence-d14-shader-dump-result.md) | Six compute stages and three graphics stages captured; SPIR-V preserves resource shapes, bounds, packing, and atomics and validates; pixel shaders consume the tile grid plus a separate light-index buffer; symptom unchanged | diagnostic only; no obvious structural translator mismatch, final buffer dependency unresolved |
| D15 | Gated post-count dispatch and all-resource legacy-barrier telemetry; commit `9c6a4338…` | [completed](evidence-d15-light-list-sync-result.md) | 1,593 complete final cycles; both the 3D grid and 87,040-byte index buffer receive two UAV dependencies and a final shader-read transition; atomics use Device scope; artifact unchanged | diagnostic only; missing synchronization excluded for this sequence, resolve fixed `t9`/`t10` descriptors next |
| D16 | Normal-path descriptor write/copy sidecar plus fixed `t9`/`t10` draw-time resolution; commit `274f6f8e…` | [completed](evidence-d16-descriptor-trace-result.md) | 13,236/13,236 lookups resolve: `t9` is the expected `80x34x2 R32_UINT` grid (cookie 4002), and `t10` is the expected 87,040-byte buffer (cookie 4001) viewed as 43,520 `R16_UINT` elements; artifact unchanged | diagnostic only; wrong selection/type/shape excluded for these inputs, values and Vulkan cache behavior remain open |
| D17 | Add RADV `startup,fullsync` to the same D16 tool and descriptor gate | [completed](evidence-d17-radv-fullsync-result.md) | Same grid artifact remains at about 10 FPS; all 8,304 descriptor lookups retain the exact D16 mappings; no device-loss/OOM/reset/hang | diagnostic only; ordinary cross-draw/dispatch synchronization and cache visibility strongly excluded, DCC and intra-shader code generation remain open |
| D18 | Add RADV `startup,nodcc` to the same D16 tool and descriptor gate | [completed](evidence-d18-radv-nodcc-result.md) | Artifact remains; user reports possibly more pronounced but is uncertain; all 6,544 descriptors retain exact mappings, no GPU error | present with magnitude inconclusive; DCC is not the sole cause or a remedy, possible visual influence retained for later matched A/B |
| D19 | Add ACO `force-waitcnt` with normal DCC/cache behavior to the same D16 tool and descriptor gate | [completed](evidence-d19-aco-force-waitcnt-result.md) | Same light-aligned grid remains; all 9,404 descriptor lookups retain the exact D16 mappings; Mesa disables pipeline caching for ACO code-generation flags, so the shaders were freshly compiled; no GPU failure | diagnostic only; ACO wait-state omission excluded, proceed to produced-value capture |
| D20 | Enable VKD3D-Proton's built-in RenderDoc integration and capture the first queue submission containing pixel shader `df0bd777fd1bb89d` | [completed](evidence-d20-renderdoc-result.md) | Two full frames show all 50 workgroups independently allocating overlapping ranges from zero; only approximately the first 320 index entries are addressed although the frames request 10,545 and 13,349 entries | diagnostic only; later D25 rejects this malformed allocation as a sufficient visual cause |
| D21 | Headless Vulkan storage-texel-buffer atomic reproducer with an SSBO control | [completed](evidence-d21-d25-atomic-result.md) | Both variants pass on RX 7800 XT and integrated Raphael RADV: counter 2,720, all values unique, one group containing zero, no validation errors | rejects a broad RADV atomic failure |
| D22 | Standalone exact-pattern execution of the captured translated shader with a legal `R32_UINT` counter | [completed](evidence-d21-d25-atomic-result.md) | Both GPUs pass through mutable descriptor sets and descriptor buffers: counter 8,160, zero overlaps/missing entries, no validation errors | rejects the exact legal SPIR-V and descriptor-buffer backend as the cause of D20's restart pattern |
| D23 | Exact shader with the game's live `R16_UINT` UAV view | [completed](evidence-d21-d25-atomic-result.md) | Both GPUs/backends deterministically reproduce corruption: 7,966 overlaps, 7,966 missing entries, all 50 groups contain zero | confirms an invalid/non-portable 32-bit atomic through a 16-bit typed view explains the malformed allocation data |
| D24 | Exact shader with only the counter translated as a raw 32-bit SSBO atomic | [completed](evidence-d21-d25-atomic-result.md) | Both GPUs/backends pass with one global 8,160-entry range and zero validation errors | proves a surgical compatibility translation repairs the standalone data pattern |
| D25 | Local per-app/per-shader VKD3D translation of `ComputeLightsFirstRef` typed UAV as SSBO | [completed, visual negative](evidence-d21-d25-atomic-result.md) | Log confirms `IL2Series.exe` quirk activation; dumped SPIR-V confirms StorageBuffer atomic; user screenshot still shows the same lit/reflected square blocks | authoritative negative: do not upstream this as a shimmering fix; test the consumer pixel shaders/later composition next |
| D26 | Override only the packed `t9` start/count fetch in both correlated pixel shaders with `uint4(0)`, taking their existing no-tiled-lights path | [completed, artifact removed](evidence-d26-tiled-light-consumer-result.md) | Both override markers are present; outputs differ by five bytes each and validate; clean screenshot preserves the aircraft and normal scene while the translucent square grid is absent | causal: artifact requires the per-light loop; diagnostic removes dynamic lights, so bisect list traversal versus per-light data/math next |
| D27 | Preserve real `t9` count/start and complete per-light evaluation, but force only the extracted `t10` light ID to valid ID 1 | [completed, artifact removed](evidence-d27-fixed-light-id-result.md) | Both override markers are present with no graphics failure; user reports the grid gone while the full loop and original iteration counts remain active | real light IDs or their selected records are required; ID 1/common loop math is safe, so preserve real IDs and filter records 2–4 next |
| D28 | Preserve real IDs 1–3 and suppress only captured ID 4 with `light_id & 3` | [completed, artifact remains](evidence-d28-filter-light-id4-result.md) | Both overrides load with no graphics failure; user reports the squares returned | excludes ID/record 4 as necessary; preserve IDs 1–2 and suppress 3–4 next |
| D29 | Preserve real IDs 1–2 and map IDs 3–4 to the existing sentinel branch | [completed, artifact remains](evidence-d29-filter-light-ids3plus-result.md) | Both overrides load with no graphics failure; user reports the squares remain | excludes IDs/records 3 and 4 as necessary; preserve only genuine ID-1 entries next to isolate ID 2 |
| D30 | Preserve only genuine ID-1 entries from the real list and map every ID 2+ to sentinel 0 | [completed, artifact remains](evidence-d30-filter-light-ids2plus-result.md) | Both overrides load with no graphics failure; user reports the squares remain | higher records are not necessary; mixed evaluation/sentinel handling or tile membership distinguishes D30 from clean D27 |
| D31 | Preserve every real nonzero light ID and replace only original zero/sentinel IDs with safe ID 1 | [completed, artifact remains](evidence-d31-replace-zero-light-id-result.md) | Both overrides load exactly once with no graphics failure; user reports the squares remain | zero/sentinel skipping is not required; at least one genuine record above ID 1 is required |
| D32 | Preserve genuine ID 2 and map every other entry to safe ID 1, with no skipped iterations | [completed, artifact remains](evidence-d32-isolate-light-id2-result.md) | Both overrides load exactly once; every iteration evaluates record 1 or record 2; user reports the squares remain with no graphics/device failure | record 2 is sufficient in the controlled mixture; resolve the adjacent `t7`/`t8` descriptor contract before changing record math |
| D33 | Passively resolve live `t7`–`t10` descriptors and the exact D3D root-range contract | [completed, artifact remains](evidence-d33-t7-t8-contract-result.md) | Build `1800206168f9d43` loads; 9,064/9,064 target lookups resolve. `t7` is the in-bounds 2,048-byte float4 record buffer, `t8` is the 4096x2048 shadow SRV, and the `t0`–`t22` root range is valid. CPU-only capture extraction finds finite record-2 data. | apparent `t7`/`t8` reversal and record-2 bounds failure excluded; discriminate record 2's shadow-comparison result from its ordinary spotlight math next |
| D34 | On the proven D32 ID-2 control, replace only the final shadow-visibility operand with existing float `1.0` | [completed, artifact remains](evidence-d34-shadow-visibility-result.md) | Both overrides load exactly once; the squares remain with no shader/GPU failure | excludes the `t8` comparison/filter result as necessary; separate record-2 math from `t9`/`t10` tile membership and multiplicity next |
| D35 | Evaluate record 2 exactly once per target pixel with constant start 0/count 1, independent of real `t9`/`t10` membership, while retaining D34's fully-visible shadow control | [completed, original squares gone](evidence-d35-global-record2-result.md) | Both overrides load exactly once; record-2 illumination is spatially smooth and overall much better; fine fading dots remain but are not tile-aligned | real tile-dependent placement/evaluation of record 2 is required; inspect the producer spotlight-culling/membership predicate rather than more consumer field bisections |
| D36 | Preserve original consumers, shadows, and all real lights, but change both producer predicates to `original_membership || light_id == 2` | [completed, artifact remains](evidence-d36-global-record2-producer-result.md) | Both producer replacements load exactly once; no D35 consumers load; the blocks return with no shader/GPU failure | rejects false-negative membership of record 2 alone; D35 also removed real record diversity, so analyse the shared producer culling logic by light class without repeating ID filters |
| D37 | Apply existing `FIXUP_RSQRT_INF_NAN` only to the exact count/index producer hashes, retaining all lights, culling, consumers, and shadows | [completed, artifact remains](evidence-d37-rsqrt-compat-result.md) | Runtime dumps prove exactly 12 added finite clamps in each target module; both validate; no old override or GPU failure | excludes exceptional reciprocal-square-root infinity/NaN as sufficient; split the later common tile-depth gate from geometric X/Y culling |
| D38 | Preserve geometric light-volume intervals for all classes but bypass only the later packed tile-depth mask/min-max rejection in both producers | [completed, block artifacts gone](evidence-d38-depth-gate-result.md) | Exactly two overrides load; no D37 quirk or graphics failure; user reports no blocky artifacts with real lighting and shadows retained | positively isolates original blocks to the common packed tile-depth rejection; the remaining fine temporal/film-grain lighting is confirmed native on Windows and accepted |
| D39 | Retain valid geometry and reconstruct the original scalar near/far overlap, bypassing only the packed logarithmic depth-mask requirement | [completed, blocks remain](evidence-d39-scalar-depth-overlap-result.md) | Exactly two intended overrides load with no GPU failure; user reports blocks and artifacts | scalar overlap is independently sufficient; test complementary packed-mask-only D40 before accepting D38 as the minimum fix |
| D40 | Retain valid geometry and packed logarithmic mask overlap, bypassing only direct scalar tile min/max overlap | [completed, blocks remain](evidence-d40-packed-mask-only-result.md) | Exactly two intended overrides load with no GPU failure; user reports blocks and artifacts | packed-mask overlap is independently sufficient; with D39, proves D38 is the minimum tested conservative compatibility behavior |
| D41 | Integrate D38 as an `IL2Series.exe` and two-shader-hash-scoped VKD3D-Proton quirk | [completed; incomplete](evidence-d42-d43-integrated-light-results.md) | The built-in depth bypass can suppress the most visible blocks, but later repeated runs and D44 prove it is not a complete list-allocation fix | retain only as part of D42/D45 until the allocator is corrected and retested |
| D42 | Combine the built-in D38 predicate with the D25 typed-UAV compatibility change in the NUMA-capable Proton tool | [completed; incomplete and intermittent](evidence-d42-d43-integrated-light-results.md) | One empty-launch view appeared free of large blocks, but broad flicker remained and later identical runs restored the blocks | D44 proves D25 selected the wrong descriptor binding; the favourable frame was transient, not a complete fix |
| D43 | Keep the original depth predicates and conservatively widen `ComputeDepthRange` packed near/far values and mask by one encoded step | [completed; failed](evidence-d42-d43-integrated-light-results.md) | Verified D43 prefix/tool DLLs; blocks return and the same lighting chain flickers | rejects one-step packed-quantization widening; capture consecutive producer and membership values before another candidate |
| D44 | Keep D42 rendering unchanged and capture three consecutive full frames with one RenderDoc trigger | [completed; allocator failure captured](evidence-d44-consecutive-capture-result.md) | Depth and grid metadata are bit-identical, but all 50 workgroups reuse offsets 0–320 for 12,126 requested entries; 69–107 overwritten light IDs change per adjacent frame | D25 emitted SSBO operations but addressed the typed descriptor set; complete the quirk by selecting the raw SSBO sibling |
| D45 | Combine the exact allocator SSBO/raw-descriptor correction with the two exact producer depth-gate bypasses | [completed; two clean runtime starts](evidence-d45-correct-ssbo-binding-result.md) | Exact D45 tool and live prefix hashes verified; user reports blocks and broad flicker completely resolved on the initial run and an independent restart with empty launch options | complete but nonminimal; D47 proves the depth bypass unnecessary |
| D46 | Attempt to retain only the allocator correction while removing D38's two depth-gate bypasses | [invalid; application wiring absent](evidence-d46-allocator-only-result.md) | Prefix provenance confirms D46 and blocks return, but source review proves `IL2Series.exe` was missing from `application_shader_quirks`; the remaining allocator quirk was inactive | do not interpret visually; build warning correctly identified the unused quirk table |
| D47 | Restore `IL2Series.exe` wiring to D46 so only the exact allocator SSBO/raw-descriptor correction is active | [completed; artifact resolved](evidence-d47-allocator-only-wired-result.md) | Prefix provenance and all four DLL hashes confirm D47; blocks and broad flicker are gone with empty launch options while original depth predicates, lighting, and shadows remain | minimal runtime-behavior proof; the first upstream architecture was later superseded by D49 |
| U01 | Fresh matched A/B of unmodified upstream master `84c87c83` against clean one-commit candidate `9b6e15be` | [completed; baseline reproduced and candidate clean](evidence-u01-upstream-candidate-ab.md) | Both isolated tools use the same NUMA-capable Wine base and differ only in four VKD3D DLLs; the baseline shows flashing tiled blocks and the candidate screenshot is clean with real lighting and shadows | historically valid mechanism and visual validation; `9b6e15be` is not the current upstream architecture |
| D49 | ABI-safe paired dxil-spirv legalization and capability-gated VKD3D-Proton integration on bases `edd8fdf7` and `731c4aae` | [completed; artifact resolved](evidence-d49-compiler-aware-result.md) | Correct D49 process path and all four DLL hashes verified; empty launch options; menu, short flight, terrain, and map are good; blocks and broad flicker are absent while real lighting and shadows remain | valid historical runtime result; superseded as an upstream design by D50-D52 and Mesa MR !43672 |
| D50 | Use one minimal coordinate-zero atomic shader, pipeline, dispatch, and 87,040-byte buffer for an `R32_UINT`, `R16_UINT`, `R32_UINT` view sequence | [completed on both RADV devices](evidence-d50-d52-r32-alias-result.md) | Both R32 runs produce one correct 2,720-value global allocation; only R16 reproduces the per-workgroup restart; validation identifies the unsupported atomic format mismatch | causal format-boundary discriminator; buffer size, reuse, pipeline, and topology excluded |
| D51 | Run exact shader `0x7cefa1bc80bb4c70` with the full 87,040-byte buffer viewed as `R32_UINT` | [completed on both RADV devices and both descriptor backends](evidence-d50-d52-r32-alias-result.md) | All four runs reach counter 8,160 with zero overlaps, missing entries, out-of-range intervals, or writes after the first counter word | exact texel-buffer path passes without SSBO or dxil-spirv lowering |
| D52 | Route only the exact IL-2 allocator UAV and shader to an `R32_UINT` texel-buffer sibling in VKD3D-Proton; dxil-spirv remains `cc75a0c9` | [completed; two visually clean runs](evidence-d50-d52-r32-alias-result.md) | Marker appears once; DXIL is byte-identical; SPIR-V retains `R32ui`, `OpImageTexelPointer`, and `OpAtomicIAdd` and changes only descriptor set/binding `1/1` to `2/0`; blocks remain absent on the diagnostic and clean repeat runs | causal runtime proof only; alias quirk is not merge-ready and is superseded by the preferred, still-open Mesa MR !43672 direction, which was not locally game-tested here |

For each completed cell, link a run directory or curated evidence file and
record new warning/error fingerprints from `compare-logs.py`.

See [`altitude-observation.md`](altitude-observation.md). If testing resumes,
E03 and later runs should use matched captures below 1,500 m and near 5,000 m
within each run.

## Startup / NUMA controls

These are a separate batch. Do not interleave them with graphics comparison
runs because a startup failure cannot evaluate rendering.

| ID | Launch environment | Prior observation | Repeatability | Classification | Evidence needed |
|---|---|---|---|---|---|
| N00 | No OpenMP override | Exact game OpenMP DLL aborts with Error #179 after MaskEx returns error 120 | Reproduced in isolated probe | causal API reproducer | Confirm with complete game on stock Proton only if another failure log is needed |
| N01 | `KMP_AFFINITY=disabled` only | Exact game OpenMP DLL initializes and reports the discovered 16 processors | Reproduced in isolated probe | bypasses defect | Retain only as a temporary launch mitigation |
| N02 | `OMP_NUM_THREADS=16` only | Same Error #179 and MaskEx error 120 as N00 | Reproduced in isolated probe | no startup remedy | Do not encode a thread count in Wine or Proton |
| N03 | Both current variables | Starts successfully | Confirmed in game and isolated probe | improved through N01 | Superseded for causal attribution by N01/N02 isolation |
| N04 | Topology-derived Wine NUMA candidate, no OpenMP override | Exact game OpenMP DLL initializes and the full game starts | Component probe and full Steam launch reproduced on the reporting host | fixed on reporting host | Repeat if needed, run Wine CI, and test other CPU/NUMA layouts |
| N05 | Exact upstream Wine MR !11604 backported to Proton 11, no launch options | Exact game OpenMP DLL initializes; full game maps the patched modules and proceeds through GUI initialization | Component probe and full Steam launch reproduced on the reporting host | fixed on reporting host; MR merged | Validate the final rebased revision when delivered through Proton, and test physical multi-node/sparse-node layouts |

Do not repeatedly trigger N00 if it risks corrupting state. Back up the prefix
first, and stop after enough evidence exists to identify the calling module.

## Run record template

Copy this block into `captures/runs/<run-id>/observations.md` (ignored by Git)
or into a curated small evidence file:

```text
Run ID:
UTC start/end:
Game build ID:
Proton version:
VKD3D-Proton commit:
DXVK commit:
Mesa/RADV version:
Kernel:
Launch options (exact):
Prefix backup ID:
Graphics settings hash/description:
Mission/camera protocol:
Started: yes/no
Menu aircraft: fixed/improved/unchanged/regressed/inconclusive
Flickering squares: fixed/improved/unchanged/regressed/inconclusive
Terrain loading: fixed/improved/unchanged/regressed/inconclusive
Distant terrain: fixed/improved/unchanged/regressed/inconclusive
Performance delta:
GPU hang/reset: yes/no
New warnings/errors:
Screenshot/video filenames and SHA-256:
Notes:
```
